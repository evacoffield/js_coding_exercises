const {
    sumDigits,
    createRange,
    getScreentimeAlertList,
    hexToRGB,
    findWinner
} = require("../challenges/exercise007");

console.log(
    sumDigits,
    createRange,
    getScreentimeAlertList,
    hexToRGB,
    findWinner);